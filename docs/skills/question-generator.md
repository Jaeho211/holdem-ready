# Question Generator Skill

이 문서는 `lib/training-data.ts`의 `questionBank`에 새 문제를 추가할 때 따라야 할 규칙과 레퍼런스입니다.
AI 코딩 도구(Gemini, Claude Code, Codex 등)가 이 문서를 읽고 일관된 품질의 문제를 생성합니다.

---

## 1. 타입 스키마

모든 문제는 `lib/training-data.ts`에 정의된 타입을 따릅니다. 추가 전에 반드시 해당 파일의 최신 타입을 확인하세요.

> **타입 임포트**: `CardCode = \`${CardRank}${CardSuit}\``, `HoleCards = readonly [CardCode, CardCode]` — `lib/holdem/cards.ts`에서 정의

### PreflopQuestion

```ts
{
  id: string;              // "pre-NNN" (3자리 0-패딩, 기존 마지막 번호 +1)
  category: "preflop";
  difficulty: "기초" | "실전" | "응용";
  title: string;           // 핵심 상황 한 줄 (ex: "UTG 첫 오픈 결정")
  prompt: string;          // 상황 설명 1~2문장, 판단 포인트 제시
  explanation: string;     // 왜 이 액션이 맞는지 2~3문장
  pitfall: string;         // 초보가 하기 쉬운 실수 한 줄
  tags: string[];          // 2개 권장 (아래 태그 목록 참조)
  hand: string;            // "AJo", "76s", "TT" 등
  holeCards: HoleCards;    // ["Ah", "Jc"] — hand의 구체적 카드 2장
  position: string;        // "UTG" | "MP" | "HJ" | "CO" | "BTN" | "SB" | "BB"
  table: string;           // "9-handed / 100bb" 형식
  stack: string;           // "100bb 유효 스택" 형식
  actionBefore: string;    // 앞선 액션 한 줄
  correct: "fold" | "call" | "raise";
}
```

### PostflopQuestion

```ts
{
  id: string;              // "post-NNN"
  category: "postflop";
  difficulty: "기초" | "실전" | "응용";
  title: string;
  prompt: string;
  explanation: string;
  pitfall: string;
  tags: string[];
  position: string;        // "UTG" | "MP" | "HJ" | "CO" | "BTN" | "SB" | "BB"
  preflopAction: string;   // "BB 콜, 나머지 폴드" — 프리플랍 라인 한 줄
  holeCards: HoleCards;    // ["Qs", "Js"] — 2장 튜플
  board: CardCode[];       // 3~5장 (플랍/턴/리버)
  pot: string;             // "12bb" 형식
  villainBet: string;      // "6bb" 또는 "15bb 체크레이즈" 등
  actionBefore: string;    // 라인 설명 한 줄
  stack: string;           // "94bb 남음" 형식
  correct: "fold" | "call" | "raise";
}
```

### OddsQuestion

```ts
{
  id: string;              // "odds-NNN"
  category: "odds";
  difficulty: "기초" | "실전" | "응용";
  title: string;
  prompt: string;
  explanation: string;
  pitfall: string;
  tags: string[];
  holeCards?: HoleCards;   // 선택 — 없으면 순수 수학 문제
  board?: CardCode[];      // 선택
  pot: string;
  villainBet: string;
  actionBefore: string;
  mathFocus: string;       // "9 Outs / Turn to River" 형식
  options: { value: string; label: string }[];  // 3개 선택지
  correct: string;         // options의 value 중 하나
}
```

---

## 2. ID 규칙

| 카테고리 | 접두사 | 예시 |
|----------|--------|------|
| preflop  | `pre-` | `pre-011`, `pre-012` |
| postflop | `post-` | `post-009`, `post-010` |
| odds     | `odds-` | `odds-007`, `odds-008` |

- **3자리 0-패딩** 사용
- 기존 마지막 ID를 확인하고 그 다음 번호 사용
- 중간 번호 삽입 금지

---

## 3. 난이도 분배

| 난이도 | 비율 | 설명 |
|--------|------|------|
| 기초 | ~40% | 명확한 정답이 있는 기본 상황 |
| 실전 | ~40% | 판단이 필요한 현실적 상황 |
| 응용 | ~20% | 복합적이거나 반직관적인 상황 |

---

## 4. 태그 가이드

### 기존 태그 (재사용 우선)

**Preflop**: 초반 포지션, 오프수트 브로드웨이, 버튼 스틸, 수딧 커넥터, SB 디펜스, BB 디펜스, 컷오프 오픈, 강한 브로드웨이, 작은 페어, 수딧 에이스, 지배당하는 핸드, 림퍼 아이솔레이트, 수딧 브로드웨이, SB 스틸, 폴드 투 유

**Postflop**: 탑페어 운영, 플랍 콜다운, 강한 메이드 핸드, 젖은 보드, 약한 드로우, 턴 폴드, 강한 드로우, 오버페어 운영, 멀티웨이

**Odds**: 아웃 계산, 턴 확률, 플랍 확률, 포트 오즈, 필요 승률, 콤보 드로우

### 추가 가능 태그 (필요 시)

**Preflop**: 3벳 팟, 4벳 팟, 포켓페어 오픈, 미들 포지션, HJ 오픈, 스퀴즈, 오버림프, 숏스택

**Postflop**: 리버 밸류벳, 리버 블러프 캐치, 체크레이즈, 돈크벳 대응, 드라이 보드, 턴 더블배럴, 셋 슬로우플레이, 보텀페어, 미들페어

**Odds**: 임플라이드 오즈, 리버스 임플라이드 오즈, 블러프 빈도, MDF, 콤보 카운팅

---

## 5. 관점과 톤

- **대상**: 라스베가스 1/3 라이브 저스테이크 초보
- **언어**: 한국어
- **톤**: 코치가 옆에서 짧게 정리해주는 느낌
- **explanation**: 왜 이 선택이 맞는지 + 라이브 필드에서의 실전 팁
- **pitfall**: "~하는 실수"로 끝나는 한 줄 패턴
- **prompt**: 상황을 제시하되 답을 유도하지 않기

---

## 6. 시나리오 커버리지 (중복 방지)

문제를 만들기 전에 기존 문제와 겹치지 않는지 확인하세요.

### Preflop — 이미 커버된 스팟

| ID | 포지션 | 핸드 | 앞선 액션 | 정답 |
|----|--------|------|-----------|------|
| pre-001 | UTG | AJo | 모두 폴드 | fold |
| pre-002 | BTN | 76s | 모두 폴드 | raise |
| pre-003 | SB | KTo | HJ 오픈 | fold |
| pre-004 | CO | AQo | 모두 폴드 | raise |
| pre-005 | BB | 55 | BTN 민레이즈 | call |
| pre-006 | MP | A5s | 모두 폴드 | raise |
| pre-007 | UTG | KJo | 모두 폴드 | fold |
| pre-008 | BTN | A9o | 림프 두 명 | raise |
| pre-009 | BB | QJs | CO 오픈 + BTN 콜 | call |
| pre-010 | SB | Q9s | 모두 폴드 | raise |
| pre-011 | HJ | TT | CO 3벳 | call |

### Postflop — 이미 커버된 스팟

| ID | 핸드 | 보드 | 상황 | 정답 |
|----|------|------|------|------|
| post-001 | QJs | Q72r | 탑페어 vs c-bet | call |
| post-002 | AA | A83ss | 셋 vs 체크레이즈 | raise |
| post-003 | JhTh | K832r | 거트샷 vs 턴 리드 | fold |
| post-004 | AhJh | Kh9h2c | 넛플드로우+오버카드 | call |
| post-005 | T7s | T78hh | 투페어 vs 돈크벳 | raise |
| post-006 | KK | 962r | 오버페어 vs 미니 체크레이즈 | call |
| post-007 | AcQd | Q954r (멀티웨이) | TPTK vs 턴 오버벳 | fold |
| post-008 | 98s | T87r | OESD+페어 vs c-bet | call |
| post-009 | K9o | K73J9r (리버) | 투페어 vs 체크 | raise |

### Odds — 이미 커버된 스팟

| ID | 주제 | 정답 |
|----|------|------|
| odds-001 | 9아웃 턴→리버 | 19% |
| odds-002 | 8아웃 플랍→리버 | 31% |
| odds-003 | 4아웃 턴→리버 | 9% |
| odds-004 | 하프팟 필요승률 | 25% |
| odds-005 | 15아웃 플랍→리버 | 54% |
| odds-006 | 20 into 80 필요승률 | 20% |
| odds-007 | 풀팟 필요승률 | 33% |

### 아직 커버되지 않은 스팟 (우선 생성 추천)

**Preflop**:
- HJ 오픈 상황
- 포켓페어(TT~JJ) 3벳 대응
- UTG 오픈에 대한 CO/BTN 콜드콜 vs 3벳
- BB에서 미니레이즈 디펜스 범위
- 림프 팟에서의 빅블라인드 레이즈

**Postflop**:
- 리버 밸류벳 결정
- 미들페어 vs 턴 더블배럴
- 드라이 보드에서의 플로팅
- 보텀페어 vs 멀티배럴
- 리버 블러프 캐치 (블라인드 vs 스틸러)

**Odds**:
- 2아웃(포켓페어 셋 확률)
- 풀팟 베팅 필요승률 (33%)
- 임플라이드 오즈 고려한 콜 판단
- 오버벳 사이즈 필요승률
- 플랍 플러시 드로우 9아웃 (≈35%)

---

## 7. 카드 표기법

- **suit**: `h`(하트), `d`(다이아), `c`(클럽), `s`(스페이드)
- **rank**: `A`, `K`, `Q`, `J`, `T`, `9`~`2`
- 예: `"Ah"` = 하트 에이스, `"Ts"` = 스페이드 10
- **핸드 표기(preflop)**: `"AJo"` (오프수트), `"76s"` (수딧), `"TT"` (페어)

---

## 8. 검증 (필수)

문제를 `questionBank`에 추가하기 **전에** 아래 검증을 반드시 수행하세요.
검증을 통과하지 못한 문제는 추가하면 안 됩니다.

### 8-A. 공통 체크리스트

- [ ] ID가 기존과 겹치지 않는가?
- [ ] 시나리오가 기존 문제와 실질적으로 다른가? (핸드, 포지션, 상황 중 2가지 이상 다를 것)
- [ ] `explanation`이 라이브 저스테이크 초보 관점에서 실질적 도움이 되는가?
- [ ] `pitfall`이 "~하는 실수" 패턴인가?
- [ ] `prompt`가 답을 유도하지 않고 상황만 제시하는가?
- [ ] 태그가 2개인가? 기존 태그를 우선 재사용했는가?
- [ ] 카드 표기가 `[rank][suit]` 형식인가?
- [ ] `correct` 값이 선택지 중 하나와 정확히 일치하는가?

### 8-B. Postflop 핸드-보드 정합성 검증

포스트플랍 문제는 `holeCards`와 `board`가 반드시 `title`, `prompt`, `explanation`에서 주장하는 핸드 강도와 일치해야 합니다.

**필수 체크 항목:**

1. **카드 중복 없음**: `holeCards`와 `board`의 모든 카드가 서로 다른가? (같은 카드가 두 번 등장하면 안 됨)
2. **핸드 강도 일치**: 설명에서 주장하는 핸드가 실제로 맞는가?
   - "탑페어" → 홀카드 중 하나의 rank가 보드 최고 rank와 같은가?
   - "투페어" → 홀카드 두 장이 각각 보드의 다른 카드와 페어인가?
   - "셋(세트)" → 홀카드가 페어이고, 보드에 그 rank가 1장 있는가?
   - "트립스" → 홀카드 1장 + 보드 2장이 같은 rank인가?
   - "오버페어" → 홀카드 페어 rank가 보드 모든 카드 rank보다 큰가?
   - "스트레이트" → 홀카드+보드 중 5장이 연속 rank인가?
   - "플러시" → 홀카드+보드 중 같은 suit가 5장 이상인가?
3. **드로우 정합성**:
   - "플러시 드로우" → 홀카드+보드에서 같은 suit가 정확히 4장인가?
   - "오픈엔디드(OESD)" → 홀카드+보드 중 4장이 연속이고 양쪽으로 완성 가능한가?
   - "거트샷" → 스트레이트 완성에 딱 1장의 특정 rank만 필요한가?
   - "넛 플러시 드로우" → 플러시 드로우이면서 그 suit의 A를 갖고 있는가?
4. **보드 텍스처 일치**: 설명에서 "젖은 보드", "드라이 보드" 등의 표현이 실제 보드와 맞는가?
   - 젖은 보드: 플러시 드로우/스트레이트 드로우 가능성이 높은 보드
   - 드라이 보드: 드로우 가능성이 거의 없는 보드 (예: K-7-2 레인보우)
5. **suit 일관성**: 보드를 "투톤(two-tone)"이라 했으면 실제로 같은 suit가 2장인가? "레인보우"라 했으면 3장이 모두 다른 suit인가?
6. **`correct` 액션 타당성**: 주장하는 핸드 강도와 상황에서 해당 액션이 포커 전략적으로 맞는가?

**검증 예시:**

```
holeCards: ["Kd", "9h"]
board: ["Ks", "7c", "3d", "Jh", "9d"]
title: "리버 투페어 밸류벳"

✅ 중복 카드 없음
✅ K페어 + 9페어 = 투페어 맞음
✅ 리버(5장)에서 완성 → 리버 상황 맞음
✅ 상대 체크 → 밸류벳(raise) 타당
```

### 8-C. Odds 수학 검증

확률/포트오즈 문제는 정답이 수학적으로 **반드시 정확해야** 합니다.

**아웃 확률 공식:**

| 상황 | 공식 | 정밀 공식 |
|------|------|-----------|
| 플랍→턴 (1장) | 아웃 × 2 | 아웃 / 47 |
| 턴→리버 (1장) | 아웃 × 2 | 아웃 / 46 |
| 플랍→리버 (2장) | 아웃 × 4 - (아웃 > 8 ? 보정) | 1 - (47-아웃)/47 × (46-아웃)/46 |

**자주 쓰는 아웃 확률 (정밀값):**

| 아웃 | 플랍→리버 | 턴→리버 |
|------|-----------|---------|
| 2 | 8.4% | 4.3% |
| 4 | 16.5% | 8.7% |
| 8 | 31.5% | 17.4% |
| 9 | 35.0% | 19.6% |
| 12 | 45.0% | 26.1% |
| 15 | 54.1% | 32.6% |

**포트 오즈 공식:**

```
필요 승률 = 콜 금액 / (기존 팟 + 상대 베팅 + 콜 금액)
```

| 베팅 사이즈 | 필요 승률 |
|-------------|-----------|
| 1/4 팟 | 16.7% |
| 1/3 팟 | 20% |
| 1/2 팟 | 25% |
| 2/3 팟 | 28.6% |
| 3/4 팟 | 30% |
| 풀팟 (1× 팟) | 33.3% |
| 1.5× 팟 | 37.5% |
| 2× 팟 | 40% |

**필수 체크 항목:**

1. **정답 수치 검산**: 위 공식/표로 직접 계산해서 `correct` 값과 대조
2. **오답 선택지 현실성**: 오답이 너무 쉽게 배제되지 않는가? (흔한 오해와 매칭되는 값이어야 함)
3. **아웃 수 정합성**: `holeCards`+`board`가 있는 경우, 주장하는 아웃 수가 실제 남은 카드와 맞는가?
   - 플러시 드로우: 13장 - 이미 보이는 해당 suit 수 = 남은 아웃
   - OESD: 양쪽 끝 각 4장 = 8아웃 (이미 보이는 카드 제외)
   - 거트샷: 4아웃 (이미 보이는 카드 제외)
4. **`explanation` 수치 일치**: 해설에 나오는 숫자가 정답 및 공식과 맞는가?
5. **라벨 형식**: `options[].label`이 "약 N%" 형식이고, `value`가 숫자 문자열인가?

**검증 예시:**

```
pot: "30bb", villainBet: "30bb"
mathFocus: "상대가 풀팟 베팅"
correct: "33"

검산: 30 / (30 + 30 + 30) = 30/90 = 0.333 → 약 33% ✅
오답 "25%"는 하프팟과 혼동하는 흔한 실수 ✅
오답 "50%"는 콜/팟 비율을 단순히 1:1로 착각하는 실수 ✅
```

---

## 9. 사용법

문제 추가를 요청할 때:

```
"preflop 문제 5개 추가해줘"
"포스트플랍 응용 난이도로 3개 만들어줘"
"odds 문제 중 임플라이드 오즈 관련으로 2개"
"커버되지 않은 스팟 위주로 각 카테고리 3개씩"
```

결과물은 `lib/training-data.ts`의 `questionBank` 배열 끝에 추가합니다.
추가 후 아래 검증을 수행하세요:

1. `npx tsc --noEmit` — 타입 에러 없는지 확인
2. `npx vitest run lib/holdem/question-bank.test.ts` — 기존 카드 검증 테스트 통과 확인

> **자동화된 테스트**: `lib/holdem/question-bank.test.ts`에 기존 문제의 카드/드로우/팟오즈 정합성을 검증하는 테스트가 있습니다. 새 postflop/odds 문제를 추가할 때 이 테스트에도 검증 케이스를 추가하는 것을 권장합니다.
